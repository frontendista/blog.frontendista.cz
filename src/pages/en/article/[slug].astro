---
import ArticlePage from "@/modules/article/article-page.astro";
import { getArticleStaticPaths } from "@/utils/i18n";

export const getStaticPaths = getArticleStaticPaths("en");

const { slug } = Astro.params;
---

<ArticlePage slug={slug} />
